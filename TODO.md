## TODO по проекту go-caching-proxy

### Основные задачи

- [ ] **Работа с Redis**
  - [ ] Подключение к Redis по конфигу (`REDIS_ADDR`, `REDIS_PASSWORD`, `REDIS_DB`)
  - [ ] Реализация `Store` на основе Redis (`Get` / `Save` / `Has` / `Remove`)
  - [ ] Выбор реализации `Store` (in-memory / Redis) через конфигурацию

- [x] **Кэширование согласно ТЗ**
  - [x] Ключ кэша: метод + нормализованный URL + query-параметры
  - [x] Кэшировать только успешные ответы (2xx)
  - [x] TTL кэша брать из конфигурации (`CACHE_TTL`), а не из захардкоженного значения

- [ ] **Инвалидация кэша**
  - [ ] Admin endpoint `POST /admin/cache/invalidate`
  - [ ] Простая авторизация по токену (`X-API-Key` / `ADMIN_TOKEN`)
  - [ ] Удаление ключей по паттерну

- [ ] **Метрики и мониторинг**
  - [ ] Интеграция `prometheus/client_golang`
  - [ ] Эндпоинт `/metrics`
  - [ ] Метрики: количество запросов, hit/miss ratio, время ответа, примерный размер кэша

- [ ] **Прокси и middleware**
  - [x] Обработка только маршрутов `/api/*` согласно ТЗ
  - [ ] Явное разделение middleware: логирование, метрики, auth для admin-эндпоинтов
  - [x] Настройка таймаутов и, при необходимости, контекста с дедлайнами

### Инфраструктура и dev-опыт

- [ ] Добавить `docker-compose.yml` для Redis и, при желании, самого сервиса
- [ ] Добавить `Makefile` / `task` / `justfile` для типовых команд (run, test, lint)
- [ ] Обновить `README` с описанием:
  - [ ] Переменных окружения текущей реализации
  - [ ] Шагов запуска in-memory и Redis-версии

### Бонусные улучшения

- [ ] Gzip-сжатие ответов для больших JSON
- [x] Rate limiting для защиты целевого API
- [ ] Healthcheck-эндпоинт, проверяющий доступность Redis и целевого API

